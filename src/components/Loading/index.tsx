import SvgIcon from '../SvgIcon';
import './index.less';

const Loading = () => {
  return (
    <div className="console-loading">
      <SvgIcon name="loading" />
    </div>
  );
};

export default Loading;
